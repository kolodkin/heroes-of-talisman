# Agent Guidelines

## Git Workflow

After each `git push`, use the `pr-checks` skill to verify GitHub Actions workflows are successful.

If any workflows fail, analyze the error logs and fix issues automatically.
